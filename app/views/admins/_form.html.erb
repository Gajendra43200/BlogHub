<%= form_with(model: admin, local: true) do |form| %>
    <% if admin.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(admin.errors.count, "error") %> prohibited this user from being saved:</h2>
  
        <ul>
          <% user.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  
    
    <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
    </div>
    <div class="field">
      <%= form.label :email %>
      <%= form.text_field :email %>
    </div>
    <div class="field">
        <%= form.label :Phone_Number %>
        <%= form.text_field :phone %>
    </div>
  
    <div class="field">
      <%= form.label :type %>
      <%= form.select :type, %w[User Admin SuperAdmin], prompt: 'Select a type' %>
  </div>
    </div>
  
    <div class="field">
      <%= form.label :password %>
      <%= form.password_field :password_digest %>
    </div>
  
    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>
  